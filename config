# sezanlight configuration file

raspberry_port=3546
gpio_r=17
gpio_g=22
gpio_b=24

raspberry_ip=

# how to read the screen:
screen_width=1920
screen_height=1080
lines=5
columns=50

# smoothing
smoothing=3
checks_per_second=2 
linear_smoothing=1

# color temperature
gamma=1.10,0.88,0.7
# brightness=1.00,0.85,0.5
# for movies, set the LEDs a bit darker (also depends on the number of LEDs):
brightness=0.50,0.425,0.25

# some other filters
increase_saturation=0.5
normalize=0

